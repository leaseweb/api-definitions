type: object
properties:
  configurationOptions:
    title: dedicatedServerConfigurationOptions
    description: Available configuration options for the dedicated server
    type: object
    properties:
      operatingSystem:
        description: Available operating system options with their price
        type: array
        items:
          title: dedicatedServerConfigurationOption
          type: object
          properties:
            name:
              description: Option name
              type: string
            selected:
              description: Whether this option is selected
              type: boolean
            price:
              description: Price for this option
              type: number
            currency:
              description: Currency for the price
              type: string
