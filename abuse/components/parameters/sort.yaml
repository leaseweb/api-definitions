name: sort
in: query
schema:
  default: The list is sorted in descending order by reportedAt
  type: string
description: >
  Comma-separated list of sort field names. Prepend the field name with '-' for
  descending order. Sortable field names are deadline, reportedAt and updatedAt
example: deadline,-reportedAt,-updatedAt
