title: FAILOVER_PORT
properties:
  routingType:
    type: string
    enum:
      - FAILOVER_PORT
  routes:
    type: array
    items:
      $ref: ./failoverPortRouting.yaml
required:
  - routingType
  - routes
type: object
