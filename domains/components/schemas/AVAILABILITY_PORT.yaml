title: AVAILABILITY_PORT
properties:
  routingType:
    type: string
    enum:
      - AVAILABILITY_PORT
  routes:
    type: array
    items:
      $ref: ./availabilityPortRouting.yaml
required:
  - routingType
  - routes
type: object
