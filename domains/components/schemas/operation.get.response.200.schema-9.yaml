$schema: http://json-schema.org/draft-07/schema#
properties:
  locks:
    type: array
    description: List of domain locks that are currently enabled for the domain
    items:
      type: string
type: object
