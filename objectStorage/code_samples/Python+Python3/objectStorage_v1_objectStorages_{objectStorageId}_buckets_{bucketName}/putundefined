import http.client

conn = http.client.HTTPSConnection("api.leaseweb.com")

payload = "{\"isVersioningEnabled\":true,\"quota\":1}"

headers = {
    'X-LSW-Auth': "REPLACE_KEY_VALUE",
    'content-type': "application/json"
    }

conn.request("PUT", "/objectStorage/v1/objectStorages/10242480/buckets/%7BbucketName%7D", payload, headers)

res = conn.getresponse()
data = res.read()

print(data.decode("utf-8"))