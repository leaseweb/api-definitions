properties:
  reason:
    description: >-
      If `reasonCode` is set to `CANCEL_OTHER`, this field is required and
      respresents the reason.
    type: string
  reasonCode:
    description: The reason code respresents a defined reason.
    type: string
type: object
