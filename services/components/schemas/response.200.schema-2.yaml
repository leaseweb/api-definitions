properties:
  cancellationReasons:
    items:
      properties:
        reason:
          description: A predefined reason of why you want to cancel a service.
          type: string
        reasonCode:
          description: >-
            The identifier of the reason. This code can be used in the payload
            of the cancel service endpoint.
          type: string
      type: object
    title: The `cancellationReasons` is an array of pre defined reasons.
    type: array
type: object
