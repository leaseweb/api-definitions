description: The type of the credential.
in: path
name: type
required: true
schema:
  type: string
  enum:
    - OPERATING_SYSTEM
    - RESCUE_MODE
    - REMOTE_MANAGEMENT
    - CONTROL_PANEL
    - SWITCH
    - PDU
    - FIREWALL
    - LOAD_BALANCER
    - VNC
    - TEMPORARY_OPERATING_SYSTEM
    - VPN_USER
    - COMBINATION_LOCK
    - DATABASE
  description: The type of the credential.
