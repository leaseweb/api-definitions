properties:
  url:
    type: string
  success:
    type: boolean
    description: True if all the provider responses are successful
  providerResponses:
    type: array
    items:
      $ref: ./providerResponse.yaml
type: object
