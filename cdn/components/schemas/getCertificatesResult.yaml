properties:
  certificates:
    type: array
    title: Certificates
    items:
      anyOf:
        - $ref: ./certificate.yaml
        - $ref: ./no_validation_certificate.yaml
  _metadata:
    $ref: ./_metadata.yaml
required:
  - certificates
  - _metadata
title: ListResponse[Certificate]
type: object
