properties:
  certificates:
    type: array
    items:
      anyOf:
        - $ref: ./certificate.yaml
        - $ref: ./noValidationCertificate.yaml
  _metadata:
    $ref: ./_metadata.yaml
required:
  - certificates
  - _metadata
type: object
