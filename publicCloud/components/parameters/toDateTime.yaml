name: to
schema:
  type: string
  format: datetime
  example: '2025-01-28T11:44:01+00:00'
in: query
description: >-
  The end of the interval to get the metric in format ISO 8601. Must be greater
  than the date provided in `from`.
